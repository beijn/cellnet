#!/bin/bash

what="model_export_* log_*.csv counts.json settings.json"

echo "This will remove the following files and directories:\n$what\nSure you want to continue? (y/n)"
read answer
if [ "$answer" != "${answer#[Yy]}" ] ;then
     echo "Removed!" && rm -rf $what
else echo "Aborted"; fi
